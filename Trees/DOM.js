//Given 2 identical DOM trees (but not equal) and one element of the first DOM tree, how would you find this element in the second DOM tree?

//input tree1, tree2, element

function findNode(node) {
  let path = [];
}
